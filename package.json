{
  "name": "keypad-recog-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Headless browser test suite for the Keypad Recognizer userscript (the script itself ships standalone; this only tests it).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
